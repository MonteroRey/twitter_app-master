<% provide(:title, 'All users') %>
<h1>All Users (All Friends)</h1>
<%= will_paginate%>
<ul class="users">
    <%@users.each do |user|%>
        <%= render user%>
    <%end%>
</ul>
<%= will_paginate%>