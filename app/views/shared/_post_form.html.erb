<%= form_with(model: @post, local: true) do |f|%>
    <div class="field">
        <%= f.text_area :content, placeholder: "Compose new Tweets ...  ", size: "50x10"%>
    </div>    
    <%= f.submit "Post", class: "btn btn-primary" %>
<%end%>